# P0-003 — Montreal Seed Routes

## Goal

Add seed route data for Montreal development and demos.

## Tasks

- Import or adapt `data/sample_montreal_routes.json`.
- Treat all sample distances/geometry as draft.
- Add enough routes to test cards, filters, comparison, detail, active walk, saved/history.
- Add route status/sourceQuality fields.

## Acceptance

- App can run with local seed data.
- At least 8 development routes are available.
- At least 4 route categories are represented.
- No screen is blocked by missing data.
